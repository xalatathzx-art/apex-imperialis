# Implants (cycle A) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give Imperium Maledictum DoomBC's implants — a real item type with quality levels, a surgical install gate, body-system slots, a mechanics constructor and a Surgeon window — replacing the module's current augmetics pipeline outright.

**Architecture:** A module-declared item sub-type `navis-apexialis.implant` extending impmal's own `augmetic` data model, so physical fields, availability and equipped state stay the system's. Pure arithmetic (limits, quality resolution, classification) lives in files with no Foundry imports and is unit-tested with `node --test`; Foundry wiring (model registration, sheet, Surgeon window, surgery) is verified in a live world. Mechanics are stored on the item as constructor entries and realised as Active Effects **on the item itself**, so removal is rollback.

**Tech Stack:** Foundry VTT 13, Imperium Maledictum 3.3.0, ES modules, `node:test` + `node:assert/strict`, Handlebars templates, plain CSS.

**Spec:** `docs/superpowers/specs/2026-09-16-implants-design.md`

## Global Constraints

- **No version control.** This module is not a git repository. Every "commit" step in the standard workflow is replaced here by a **checkpoint**: run the stated verification, confirm the output, and stop for review. Before any deletion, copy the target to the session scratchpad first.
- **Module id** is `navis-apexialis`. The item type id is exactly `navis-apexialis.implant`.
- **Never modify the impmal system.** Everything extends `game.impmal.config` or `CONFIG.*`; the module must stay switch-off-able.
- **Do not touch the native `augmetic` type or `system.augmetics.max`.** Two independent types, two independent budgets.
- **Foundry ids are exactly 16 alphanumeric characters.** Not 15, not 17. A wrong-length id fails validation at world launch, is migrated to `_id: null`, and takes the whole compendium down silently until the world will not open.
- **Content text is Russian**, following `docs/rules/ru-glossary.md`. Code, comments in new files, and this plan's identifiers are English, matching `module/species/` and `module/technomiracles/`.
- **Conversion doctrine**, applied everywhere without exception:
  - ±10 on a d100 → +1 / −1 success
  - ±20 → +2 / −2 successes
  - ±30 and beyond → Advantage / Disadvantage
  - Unnatural (X) → +5 per two points
  - FFG Wounds → ÷4
  - AP → AP, unchanged
  - Poor / Comm / Good / Best → Уровень 1 / 2 / 3 / 4, where 2 is the ordinary article
  - Haywire and Ноосфера → prose only
- **Test command** is `node --test tests/<file>` from the module root. There is no `package.json` and no test framework beyond `node:test`.
- **Families in scope:** bionics, cybernetics, mechadendrites, Mechanicum, Skitarii, psybernetics. Drukhari bio-implants and Astartes gene-seed organs are cycle B and must not appear.

---

### Task 1: Body-system classifier

Resolves an implant's name to a body **system** (`slot`) and from there to one of impmal's six hit locations (`location`). Pure — no Foundry imports, no document access.

**Files:**
- Create: `module/implants/classify.js`
- Test: `tests/implants-classify.test.mjs`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `SLOTS` — frozen array of `{ key, label }`, keys: `cortex`, `ocular`, `respiratory`, `circulatory`, `skeleton`, `skin`, `arm`, `leg`, `mechadendrite`, `fullbody`, `other`
  - `SLOT_LOCATION` — frozen map `slot key → "head"|"body"|"limb"`; `"limb"` means the caller resolves by side
  - `classifyImplant(name, category, installedText) → string` (a `SLOTS` key, never null; unmatched returns `"other"`)
  - `locationForSlot(slot, side) → "head"|"body"|"leftArm"|"rightArm"|"leftLeg"|"rightLeg"|"internal"`

- [ ] **Step 1: Write the failing test**

Create `tests/implants-classify.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { SLOTS, SLOT_LOCATION, classifyImplant, locationForSlot } from "../module/implants/classify.js";

test("slot keys are the eleven body systems the Surgeon lays out", () => {
  assert.deepEqual(SLOTS.map(s => s.key), [
    "cortex", "ocular", "respiratory", "circulatory", "skeleton",
    "skin", "arm", "leg", "mechadendrite", "fullbody", "other"
  ]);
});

test("every slot has a location rule", () => {
  for (const slot of SLOTS) assert.ok(SLOT_LOCATION[slot.key], `no location for ${slot.key}`);
});

test("category wins over the name where it is already precise", () => {
  // Серво-Коготь contains "коготь" and would otherwise be read as a leg.
  assert.equal(classifyImplant("Серво-Коготь", "mechadendrite", ""), "mechadendrite");
});

test("Russian and English names both classify", () => {
  assert.equal(classifyImplant("Bionic Arm", "", ""), "arm");
  assert.equal(classifyImplant("Бионическая рука", "", ""), "arm");
  assert.equal(classifyImplant("Cortical Implant", "", ""), "cortex");
  assert.equal(classifyImplant("Кортикальный Имплант", "", ""), "cortex");
  assert.equal(classifyImplant("Memorance Implant", "", ""), "cortex");
  assert.equal(classifyImplant("Ranger Visors", "", ""), "ocular");
  assert.equal(classifyImplant("Визоры Рейнджера", "", ""), "ocular");
  assert.equal(classifyImplant("Бионическое Сердце", "", ""), "circulatory");
  assert.equal(classifyImplant("Когти Птераксии", "", ""), "leg");
  assert.equal(classifyImplant("Сикарианские ЭФМ", "", ""), "skeleton");
});

test("the ordering traps from the reference system are preserved", () => {
  // "Сус-ан Мембрана" must not fall into skin on the word "мембрана".
  assert.equal(classifyImplant("Сус-ан Мембрана", "", ""), "cortex");
  // "Железы Бетчера" must not fall into the torso.
  assert.equal(classifyImplant("Железы Бетчера", "", ""), "cortex");
});

test("full-body kits are their own slot", () => {
  assert.equal(classifyImplant("Боевые Латы Скитарии (всё тело)", "", ""), "fullbody");
});

test("an unmatched name lands in other, never nowhere", () => {
  assert.equal(classifyImplant("Нечто Безымянное", "", ""), "other");
});

test("locations resolve to impmal hit locations, limbs by side", () => {
  assert.equal(locationForSlot("cortex", ""), "head");
  assert.equal(locationForSlot("ocular", ""), "head");
  assert.equal(locationForSlot("circulatory", ""), "body");
  assert.equal(locationForSlot("arm", "left"), "leftArm");
  assert.equal(locationForSlot("arm", "right"), "rightArm");
  assert.equal(locationForSlot("leg", "left"), "leftLeg");
  assert.equal(locationForSlot("leg", "right"), "rightLeg");
  assert.equal(locationForSlot("fullbody", ""), "body");
  assert.equal(locationForSlot("other", ""), "internal");
});

test("a limb with no side chosen is internal rather than guessing a side", () => {
  assert.equal(locationForSlot("arm", ""), "internal");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-classify.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/classify.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/classify.js`:

```js
/**
 * Name → body system → hit location.
 *
 * Ported from warhammer-dbc `module/constants/body-map.mjs::classifyImplant`,
 * with one deliberate change: there the classifier is consulted live, so
 * renaming an implant moves it to another body part with no error. Here it is
 * a DEFAULT used at authoring time — the generator writes `slot` and
 * `location` into the item, and the sheet lets both be overridden. The name is
 * not an identifier.
 *
 * Order inside IMPLANT_SLOTS matters and is not alphabetical: `cortex` sits
 * above `skin` so "Сус-ан Мембрана" is not read as skin on the word
 * "мембрана", and above `circulatory` so "Железы Бетчера" is not read as
 * torso. First match wins.
 */

export const SLOTS = Object.freeze([
  { key: "cortex", label: "NAVIS.Implant.Slot.cortex" },
  { key: "ocular", label: "NAVIS.Implant.Slot.ocular" },
  { key: "respiratory", label: "NAVIS.Implant.Slot.respiratory" },
  { key: "circulatory", label: "NAVIS.Implant.Slot.circulatory" },
  { key: "skeleton", label: "NAVIS.Implant.Slot.skeleton" },
  { key: "skin", label: "NAVIS.Implant.Slot.skin" },
  { key: "arm", label: "NAVIS.Implant.Slot.arm" },
  { key: "leg", label: "NAVIS.Implant.Slot.leg" },
  { key: "mechadendrite", label: "NAVIS.Implant.Slot.mechadendrite" },
  { key: "fullbody", label: "NAVIS.Implant.Slot.fullbody" },
  { key: "other", label: "NAVIS.Implant.Slot.other" }
]);

/** "limb" means the location depends on which side the implant is fitted. */
export const SLOT_LOCATION = Object.freeze({
  cortex: "head",
  ocular: "head",
  respiratory: "body",
  circulatory: "body",
  skeleton: "body",
  skin: "body",
  arm: "limb",
  leg: "limb",
  mechadendrite: "body",
  fullbody: "body",
  other: "internal"
});

/** Slots whose location is decided by `side`, and the zones they resolve to. */
const LIMB_ZONES = Object.freeze({
  arm: { left: "leftArm", right: "rightArm" },
  leg: { left: "leftLeg", right: "rightLeg" }
});

/** Category values that already say the slot outright — more reliable than any regex. */
const CATEGORY_SLOT = Object.freeze({
  mechadendrite: "mechadendrite",
  "bionic-arm": "arm",
  "bionic-leg": "leg"
});

const FULLBODY = /всё тело|все тело|полностью|whole body|full body|all body/i;

const IMPLANT_SLOTS = Object.freeze([
  ["mechadendrite", /servo-manip|манипул|mechadendr|механодендр|мехатендр|servo-arm|dataspike|даташип|scribe-tine|писар|prehensile|цепк|gordii|гордий/i],
  ["ocular", /\beye\b|глаз|ocular|окуляр|visor|визор|auspex|ауспекс|оккулоб|occulobe/i],
  // Above skin and circulatory on purpose — see the file header.
  ["cortex", /cranial|черепн|cortical|кортикал|cerebr|мозж|mind|мысл|разум|memoranc|меморанс|\bmiu\b|neuro|нейро|skull|череп|infoslave|инфораб|volitor|волитор|catechism|катехиз|noospher|ноосфер|logic|логик|каталепт|catalepsean|сус-ан|sus-an|бетчер|betcher/i],
  ["circulatory", /heart|сердц|autosangu|автосангв|potentia|потенциа|гемастамен|haemastamen|ларраман|larraman/i],
  ["respiratory", /respirator|респират|breath|дыхан|воздух|противогаз|rebreath|lung|лёгк|легк/i],
  ["arm", /\barm\b|рук(а|у|и|е)|iron fist|железн[а-яё]* кулак|iron hand/i],
  ["leg", /\bleg\b|ног(а|и|у|е)|tracked|гусениц|digitigrad|дигитигр|all-?terrain|вездеход|talon|коготь|птераксии|pteraxii|arachnid|арахнид|serpentine|серпентин|repulsor|репульсор/i],
  ["skeleton", /skeletal|скелет|petrif|укреплен|spined|шипаст|\bbone\b|кост(ь|и|ей|ный)|adamant|адамант|sicarian efm|сикарианск|эфм|оссмодул|ossmodul/i],
  ["skin", /subdermal|подкожн|voidskin|пустотн[а-яё]* кож|synthmusc|синтемускул|\bdermal\b|scale skin|чешу|membrane|мембран|меланохром|melanochrom|мукраноид|mucranoid/i]
]);

/**
 * @param {string} name       the implant's name
 * @param {string} category   system.category, consulted first where precise
 * @param {string} installedText  free-text placement note, searched alongside the name
 * @returns {string} a SLOTS key — always one, never null
 */
export function classifyImplant(name = "", category = "", installedText = "") {
  if (CATEGORY_SLOT[category]) return CATEGORY_SLOT[category];

  const hay = `${name} ${installedText}`;
  if (FULLBODY.test(hay)) return "fullbody";

  for (const [slot, re] of IMPLANT_SLOTS) if (re.test(hay)) return slot;
  return "other";
}

/**
 * @param {string} slot  a SLOTS key
 * @param {string} side  "left" | "right" | ""
 * @returns {string} an impmal hit location, or "internal"
 */
export function locationForSlot(slot, side = "") {
  const rule = SLOT_LOCATION[slot];
  if (!rule) return "internal";
  // A limb with no side chosen is internal rather than guessing: guessing would
  // silently give the wrong arm its armour bonus.
  if (rule === "limb") return LIMB_ZONES[slot]?.[side] ?? "internal";
  return rule;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-classify.test.mjs`
Expected: PASS, 9 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-classify.test.mjs`
Confirm: `pass 9`, `fail 0`. Stop for review.

---

### Task 2: Limits and quality arithmetic

The ½ T.b cap, the separate active cap, the over-cap penalty, and resolving a per-level value to a number. Pure — no Foundry imports.

**Files:**
- Create: `module/implants/rules.js`
- Test: `tests/implants-rules.test.mjs`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `installedCap(toughnessBonus, talentBonus = 0, sacredCode = false) → number`
  - `activeCap(toughnessBonus, talentBonus = 0) → number`
  - `capState({ installed, active, toughnessBonus, talentBonus, sacredCode }) → { installedCap, activeCap, overInstalled, overActive, penalty }` where `penalty` is `"disadvantage"` or `""`
  - `resolveQualityValue(value, quality) → number` — `value` may be a plain number or `{1,2,3,4}`
  - `QUALITY_LEVELS` — frozen `[1, 2, 3, 4]`
  - `ORDINARY_QUALITY` — `2`

- [ ] **Step 1: Write the failing test**

Create `tests/implants-rules.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import {
  QUALITY_LEVELS, ORDINARY_QUALITY,
  installedCap, activeCap, capState, resolveQualityValue
} from "../module/implants/rules.js";

test("the cap is half the Toughness bonus, rounded up (DoomBC p. 269)", () => {
  assert.equal(installedCap(0), 0);
  assert.equal(installedCap(1), 1);
  assert.equal(installedCap(2), 1);
  assert.equal(installedCap(3), 2);
  assert.equal(installedCap(4), 2);
  assert.equal(installedCap(5), 3);
  assert.equal(installedCap(7), 4);
});

test("a negative or missing Toughness bonus is a cap of zero, not a negative cap", () => {
  assert.equal(installedCap(-3), 0);
  assert.equal(installedCap(undefined), 0);
});

test("the talent raises both caps by one (DoomBC p. 102)", () => {
  assert.equal(installedCap(4, 1), 3);
  assert.equal(activeCap(4, 1), 3);
});

test("the Sacred Code adds two INSTALLED only, never active", () => {
  assert.equal(installedCap(4, 0, true), 4);
  assert.equal(activeCap(4, 0), 2);
});

test("within both caps there is no penalty", () => {
  const s = capState({ installed: 2, active: 2, toughnessBonus: 4 });
  assert.equal(s.installedCap, 2);
  assert.equal(s.activeCap, 2);
  assert.equal(s.overInstalled, 0);
  assert.equal(s.overActive, 0);
  assert.equal(s.penalty, "");
});

test("too many active modules is Disadvantage on everything (−30 under the doctrine)", () => {
  const s = capState({ installed: 2, active: 3, toughnessBonus: 4 });
  assert.equal(s.overActive, 1);
  assert.equal(s.penalty, "disadvantage");
});

test("a dropping Toughness bonus puts a legal character over the cap", () => {
  const before = capState({ installed: 3, active: 3, toughnessBonus: 5 });
  assert.equal(before.penalty, "");
  const after = capState({ installed: 3, active: 3, toughnessBonus: 3 });
  assert.equal(after.overActive, 1);
  assert.equal(after.penalty, "disadvantage");
});

test("the Sacred Code lets you carry excess installed without penalty, but not active", () => {
  const s = capState({ installed: 4, active: 2, toughnessBonus: 4, sacredCode: true });
  assert.equal(s.overInstalled, 0);
  assert.equal(s.overActive, 0);
  assert.equal(s.penalty, "");
});

test("quality levels are 1..4 and 2 is the ordinary article", () => {
  assert.deepEqual(QUALITY_LEVELS, [1, 2, 3, 4]);
  assert.equal(ORDINARY_QUALITY, 2);
});

test("a plain number is the same at every level", () => {
  for (const q of QUALITY_LEVELS) assert.equal(resolveQualityValue(5, q), 5);
});

test("a per-level value picks its level", () => {
  const ladder = { 1: 1, 2: 3, 3: 5, 4: 7 };
  assert.equal(resolveQualityValue(ladder, 1), 1);
  assert.equal(resolveQualityValue(ladder, 2), 3);
  assert.equal(resolveQualityValue(ladder, 3), 5);
  assert.equal(resolveQualityValue(ladder, 4), 7);
});

test("a missing level falls back to the ordinary article, not to zero", () => {
  assert.equal(resolveQualityValue({ 2: 3 }, 4), 3);
});

test("nothing at all is zero", () => {
  assert.equal(resolveQualityValue(undefined, 2), 0);
  assert.equal(resolveQualityValue({}, 2), 0);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-rules.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/rules.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/rules.js`:

```js
/**
 * Everything about implants that is decided by arithmetic rather than by a
 * Foundry document. Nothing here is imported from Foundry, so it is testable
 * without a running game — the same division `module/technomiracles/rules.js`
 * uses.
 *
 * Two book rules live here.
 *
 * The cap (DoomBC p. 269): a character may carry no more than ½ T.b (rounded
 * up) Modules. If the Toughness bonus drops — from Toughness damage, say — the
 * excess must be switched OFF, or everything is at −30. Under this module's
 * conversion doctrine (±30 and beyond → Disadvantage) that penalty is
 * Disadvantage on all tests.
 *
 * Installed and active are separate ceilings (DoomBC p. 102): the Mysteries of
 * the Sacred Code let a character FIT two more than the limit, which does not
 * raise how many may be ACTIVE — it only allows reconfiguring on the fly.
 */

export const QUALITY_LEVELS = Object.freeze([1, 2, 3, 4]);

/** Level 2 is the ordinary article; the book only prints the levels that differ from it. */
export const ORDINARY_QUALITY = 2;

/** The Sacred Code's allowance, in extra INSTALLED modules only. */
const SACRED_CODE_BONUS = 2;

const atLeastZero = n => Math.max(0, Number.isFinite(n) ? n : 0);

/** ½ T.b rounded up, never below zero, plus whatever talents grant. */
function baseCap(toughnessBonus, talentBonus) {
  return Math.ceil(atLeastZero(toughnessBonus) / 2) + atLeastZero(talentBonus);
}

export function installedCap(toughnessBonus, talentBonus = 0, sacredCode = false) {
  return baseCap(toughnessBonus, talentBonus) + (sacredCode ? SACRED_CODE_BONUS : 0);
}

export function activeCap(toughnessBonus, talentBonus = 0) {
  return baseCap(toughnessBonus, talentBonus);
}

/**
 * @param {object} input
 * @param {number} input.installed       how many implants are fitted
 * @param {number} input.active          how many of those are switched on
 * @param {number} input.toughnessBonus
 * @param {number} [input.talentBonus]
 * @param {boolean} [input.sacredCode]
 * @returns {{installedCap: number, activeCap: number, overInstalled: number,
 *            overActive: number, penalty: ""|"disadvantage"}}
 */
export function capState({ installed, active, toughnessBonus, talentBonus = 0, sacredCode = false }) {
  const iCap = installedCap(toughnessBonus, talentBonus, sacredCode);
  const aCap = activeCap(toughnessBonus, talentBonus);

  const overInstalled = Math.max(0, atLeastZero(installed) - iCap);
  const overActive = Math.max(0, atLeastZero(active) - aCap);

  return {
    installedCap: iCap,
    activeCap: aCap,
    overInstalled,
    overActive,
    penalty: (overInstalled || overActive) ? "disadvantage" : ""
  };
}

/**
 * A constructor entry's value, which may depend on the implant's quality.
 *
 * A plain number is the same at every level. An object is a ladder keyed 1..4.
 * A level the ladder does not mention falls back to the ordinary article rather
 * than to zero: the book writes only the levels that DIFFER from level 2, so a
 * missing level means "same as ordinary", not "nothing".
 */
export function resolveQualityValue(value, quality) {
  if (typeof value === "number") return Number.isFinite(value) ? value : 0;
  if (!value || typeof value !== "object") return 0;

  const exact = value[quality];
  if (Number.isFinite(exact)) return exact;

  const ordinary = value[ORDINARY_QUALITY];
  return Number.isFinite(ordinary) ? ordinary : 0;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-rules.test.mjs`
Expected: PASS, 13 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-rules.test.mjs`
Confirm: `pass 13`, `fail 0`. Stop for review.

---

### Task 3: The gate

One predicate deciding whether an implant is doing anything right now, plus the counters the cap rules need. Pure — it takes plain objects, not Foundry documents, so it is testable directly.

**Files:**
- Create: `module/implants/state.js`
- Test: `tests/implants-state.test.mjs`

**Interfaces:**
- Consumes: `capState` from `module/implants/rules.js`.
- Produces:
  - `IMPLANT_TYPE` — the string `"navis-apexialis.implant"`
  - `isImplantActive(item) → boolean`
  - `isImplantFitted(item) → boolean`
  - `implantsOf(actor) → array` — the actor's implant items
  - `actorCapState(actor) → ReturnType<capState>`

- [ ] **Step 1: Write the failing test**

Create `tests/implants-state.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { IMPLANT_TYPE, isImplantActive, isImplantFitted, implantsOf, actorCapState } from "../module/implants/state.js";

const implant = (installed, disabled, active) => ({
  type: IMPLANT_TYPE,
  system: { installed, disabled, active }
});

test("the type id is the one declared in module.json", () => {
  assert.equal(IMPLANT_TYPE, "navis-apexialis.implant");
});

test("an implant acts only when fitted, undamaged and switched on", () => {
  assert.equal(isImplantActive(implant(true, false, true)), true);
  assert.equal(isImplantActive(implant(false, false, true)), false, "not fitted");
  assert.equal(isImplantActive(implant(true, true, true)), false, "damaged");
  assert.equal(isImplantActive(implant(true, false, false)), false, "switched off");
});

test("fitted is independent of switched on — the book counts them separately", () => {
  assert.equal(isImplantFitted(implant(true, false, false)), true);
  assert.equal(isImplantActive(implant(true, false, false)), false);
});

test("a damaged implant is still fitted and still occupies its socket", () => {
  assert.equal(isImplantFitted(implant(true, true, true)), true);
});

test("a malformed item is inert rather than throwing", () => {
  assert.equal(isImplantActive(undefined), false);
  assert.equal(isImplantActive({}), false);
  assert.equal(isImplantFitted({ system: {} }), false);
});

test("only implants are counted, never native augmetics", () => {
  const actor = { items: [implant(true, false, true), { type: "augmetic", system: {} }] };
  assert.equal(implantsOf(actor).length, 1);
});

test("the actor's cap state counts fitted and active separately", () => {
  const actor = {
    system: { characteristics: { tgh: { bonus: 4 } } },
    items: [
      implant(true, false, true),
      implant(true, false, true),
      implant(true, false, false)
    ]
  };
  const state = actorCapState(actor);
  assert.equal(state.installedCap, 2);
  assert.equal(state.activeCap, 2);
  assert.equal(state.overInstalled, 1, "three fitted against a cap of two");
  assert.equal(state.overActive, 0, "only two are switched on");
  assert.equal(state.penalty, "disadvantage");
});

test("an actor with no implants is never penalised", () => {
  const actor = { system: { characteristics: { tgh: { bonus: 0 } } }, items: [] };
  assert.equal(actorCapState(actor).penalty, "");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-state.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/state.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/state.js`:

```js
/**
 * The gate: is this implant doing anything right now.
 *
 * Everything an implant grants hangs off `isImplantActive` — Active Effects
 * created on the item synchronise their own `disabled` to it, and live-request
 * entries consult it at roll time. One predicate, one source of truth. This is
 * warhammer-dbc's `isItemActive` + `syncItemEffectsDisabled` pair narrowed to a
 * single type.
 *
 * State lives in schema fields, not document flags. warhammer-dbc uses flags
 * because it was bolting state onto a type it wanted to keep schema-stable;
 * here the type is ours, and fields are what a data model is for.
 *
 * These functions take plain objects, never Foundry documents, so the whole
 * gate is testable without a running game.
 */

import { capState } from "./rules.js";

export const IMPLANT_TYPE = "navis-apexialis.implant";

/** Fitted — surgically installed. Damaged implants are still fitted: they still occupy the socket. */
export function isImplantFitted(item) {
  return !!item?.system?.installed;
}

/** Acting — fitted, undamaged, and switched on. */
export function isImplantActive(item) {
  const sys = item?.system;
  return !!sys?.installed && !sys?.disabled && !!sys?.active;
}

/** The actor's implants. Native `augmetic` items are a different type and a different budget. */
export function implantsOf(actor) {
  return (actor?.items ?? []).filter(item => item?.type === IMPLANT_TYPE);
}

/** Where this actor stands against both ceilings. */
export function actorCapState(actor) {
  const implants = implantsOf(actor);

  return capState({
    installed: implants.filter(isImplantFitted).length,
    active: implants.filter(isImplantActive).length,
    toughnessBonus: actor?.system?.characteristics?.tgh?.bonus ?? 0,
    talentBonus: 0,
    sacredCode: false
  });
}
```

Note for the implementer: `talentBonus` and `sacredCode` are wired to the actual talents in Task 8, once the constructor can grant them. Leaving them hard-coded here keeps this task's test honest about what is and is not implemented.

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-state.test.mjs`
Expected: PASS, 8 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-classify.test.mjs tests/implants-rules.test.mjs tests/implants-state.test.mjs`
Confirm: `pass 30`, `fail 0`. Stop for review.

---

### Task 4: Mechanics targets

The kind → impmal data path table, and resolution of an entry to a concrete Active Effect change. Pure.

**Files:**
- Create: `module/implants/mechanics/targets.js`
- Test: `tests/implants-targets.test.mjs`

**Interfaces:**
- Consumes: `resolveQualityValue` from `module/implants/rules.js`.
- Produces:
  - `ENTRY_KINDS` — frozen array of kind keys
  - `LIVE_KINDS` — frozen array of the kinds that write nothing and are read at roll time
  - `targetPath(entry) → string|null`
  - `entryToChange(entry, quality) → { key, mode, value, priority }|null`

- [ ] **Step 1: Write the failing test**

Create `tests/implants-targets.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { ENTRY_KINDS, LIVE_KINDS, targetPath, entryToChange } from "../module/implants/mechanics/targets.js";

test("the entry kinds are the set cycle A's families need", () => {
  assert.deepEqual([...ENTRY_KINDS].sort(), [
    "armour", "armourAll", "characteristic", "criticals", "encumbrance",
    "energy", "script", "skill", "speed", "talent", "testMod", "trait", "wounds"
  ]);
});

test("live kinds write nothing and are read at roll time", () => {
  assert.deepEqual([...LIVE_KINDS].sort(), ["script", "testMod"]);
  for (const kind of LIVE_KINDS) assert.equal(targetPath({ kind }), null);
});

test("characteristics and skills target impmal's own modifier fields", () => {
  assert.equal(targetPath({ kind: "characteristic", key: "tgh" }), "system.characteristics.tgh.modifier");
  assert.equal(targetPath({ kind: "skill", key: "medicae" }), "system.skills.medicae.modifier");
});

test("armour targets the six hit locations impmal already has", () => {
  assert.equal(targetPath({ kind: "armour", key: "leftArm" }), "system.combat.hitLocations.leftArm.armour");
  assert.equal(targetPath({ kind: "armourAll" }), "system.combat.armourModifier");
});

test("wounds, criticals, speed and encumbrance target their real paths", () => {
  assert.equal(targetPath({ kind: "wounds" }), "system.combat.wounds.max");
  assert.equal(targetPath({ kind: "criticals" }), "system.combat.criticals.max");
  assert.equal(targetPath({ kind: "speed", key: "land" }), "system.combat.speed.land.modifier");
  assert.equal(targetPath({ kind: "speed", key: "fly" }), "system.combat.speed.fly.modifier");
  assert.equal(targetPath({ kind: "encumbrance", key: "overburdened" }), "system.encumbrance.overburdened");
});

test("Заряд has no data path — an Active Effect cannot reach a module flag", () => {
  assert.equal(targetPath({ kind: "energy" }), null);
  assert.equal(entryToChange({ kind: "energy", value: 5 }, 2), null);
});

test("grants have no data path — they create items instead", () => {
  assert.equal(targetPath({ kind: "trait" }), null);
  assert.equal(targetPath({ kind: "talent" }), null);
});

test("an unknown kind resolves to nothing rather than to a wrong path", () => {
  assert.equal(targetPath({ kind: "nonsense" }), null);
});

test("an entry becomes an additive change at its quality level", () => {
  const change = entryToChange({ kind: "characteristic", key: "str", value: 10 }, 2);
  assert.deepEqual(change, {
    key: "system.characteristics.str.modifier",
    mode: 2,
    value: 10,
    priority: 20
  });
});

test("a per-level value resolves before becoming a change", () => {
  const entry = { kind: "wounds", value: { 1: 1, 2: 3, 3: 5, 4: 7 } };
  assert.equal(entryToChange(entry, 1).value, 1);
  assert.equal(entryToChange(entry, 4).value, 7);
});

test("a live entry produces no change at all", () => {
  assert.equal(entryToChange({ kind: "testMod", value: 1 }, 2), null);
});

test("a zero value produces no change — an effect that does nothing is noise on the sheet", () => {
  assert.equal(entryToChange({ kind: "wounds", value: 0 }, 2), null);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-targets.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/mechanics/targets.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/mechanics/targets.js`:

```js
/**
 * Where a constructor entry points.
 *
 * This file is the whole translation layer between DoomBC's vocabulary and
 * Imperium Maledictum's data paths, and it holds nothing else — adding a target
 * is a one-line change in one place.
 *
 * Every path here was taken from impmal's own effect-key list
 * (`templates/apps/effect-key-options.hbs`), so an implant reaches exactly the
 * fields the system already lets an Active Effect reach. The one exception is
 * `energy`: Заряд does not exist in impmal at all and lives in a module flag.
 *
 * Two kinds — `testMod` and `script` — deliberately have NO path. Success
 * bonuses and Advantage are not effect keys in impmal; they are decided when a
 * test is prepared, by reading `args.fields`. So those entries write nothing
 * and are consulted live at roll time.
 */

import { resolveQualityValue } from "../rules.js";

/** Foundry's CONST.ACTIVE_EFFECT_MODES.ADD, inlined so this file needs no Foundry import. */
const MODE_ADD = 2;

/** After impmal's own derived values, before anything that reads a final total. */
const PRIORITY = 20;

export const ENTRY_KINDS = Object.freeze([
  "characteristic", "skill", "armour", "armourAll",
  "wounds", "criticals", "speed", "encumbrance",
  "energy", "trait", "talent", "testMod", "script"
]);

/** Kinds that store nothing and are read at the moment of a roll. */
export const LIVE_KINDS = Object.freeze(["testMod", "script"]);

/** Kinds that create an item on the actor rather than changing a number. */
const GRANT_KINDS = Object.freeze(["trait", "talent"]);

/**
 * Kinds computed directly rather than applied as an effect. Заряд lives in a
 * module flag, and an Active Effect cannot reach one — module/technomiracles/
 * resources.js records this from an earlier cycle. Task 12's energyCapacity()
 * walks these entries itself, so producing a change here would be dead at best
 * and a double-count at worst.
 */
const COMPUTED_KINDS = Object.freeze(["energy"]);

const FIXED_PATHS = Object.freeze({
  armourAll: "system.combat.armourModifier",
  wounds: "system.combat.wounds.max",
  criticals: "system.combat.criticals.max"
});

const KEYED_PATHS = Object.freeze({
  characteristic: key => `system.characteristics.${key}.modifier`,
  skill: key => `system.skills.${key}.modifier`,
  armour: key => `system.combat.hitLocations.${key}.armour`,
  speed: key => `system.combat.speed.${key}.modifier`,
  encumbrance: key => `system.encumbrance.${key}`
});

/**
 * @param {{kind: string, key?: string}} entry
 * @returns {string|null} the data path, or null for live and grant kinds
 */
export function targetPath(entry) {
  const kind = entry?.kind;
  if (!kind) return null;
  if (LIVE_KINDS.includes(kind) || GRANT_KINDS.includes(kind) || COMPUTED_KINDS.includes(kind)) return null;

  if (FIXED_PATHS[kind]) return FIXED_PATHS[kind];

  const build = KEYED_PATHS[kind];
  if (!build || !entry.key) return null;
  return build(entry.key);
}

/**
 * @param {object} entry   a constructor entry
 * @param {number} quality the implant's level, 1..4
 * @returns {{key: string, mode: number, value: number, priority: number}|null}
 */
export function entryToChange(entry, quality) {
  const key = targetPath(entry);
  if (!key) return null;

  const value = resolveQualityValue(entry?.value, quality);
  // An effect that adds nothing is noise on the sheet, not a neutral no-op.
  if (!value) return null;

  return { key, mode: MODE_ADD, value, priority: PRIORITY };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-targets.test.mjs`
Expected: PASS, 12 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-targets.test.mjs`
Confirm: `pass 12`, `fail 0`. Stop for review.

---

### Task 5: Group resolution

An implant's mechanics are groups of entries; AND applies all, OR applies one chosen. This task is the pure half — deciding what should be applied. Creating documents is Task 7.

**Files:**
- Create: `module/implants/mechanics/entries.js`
- Test: `tests/implants-entries.test.mjs`

**Interfaces:**
- Consumes: `entryToChange`, `LIVE_KINDS` from `./targets.js`.
- Produces:
  - `needsChoice(mechanics) → boolean`
  - `pendingChoices(mechanics, chosen) → array of groups still awaiting a choice`
  - `resolveEntries(mechanics, chosen) → array of entries to apply`
  - `changesFor(mechanics, chosen, quality) → array of Active Effect changes`
  - `liveEntries(mechanics, chosen) → array of live entries`

`chosen` is a map `{ [groupId]: entryId }`.

- [ ] **Step 1: Write the failing test**

Create `tests/implants-entries.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { needsChoice, pendingChoices, resolveEntries, changesFor, liveEntries } from "../module/implants/mechanics/entries.js";

const AND_GROUP = {
  id: "g1",
  operator: "AND",
  entries: [
    { id: "e1", kind: "characteristic", key: "tgh", value: 5 },
    { id: "e2", kind: "armour", key: "leftArm", value: 1 }
  ]
};

const OR_GROUP = {
  id: "g2",
  operator: "OR",
  entries: [
    { id: "e3", kind: "skill", key: "athletics", value: 10 },
    { id: "e4", kind: "skill", key: "stealth", value: 10 }
  ]
};

test("an AND group needs no choice", () => {
  assert.equal(needsChoice([AND_GROUP]), false);
});

test("an OR group needs a choice until one is made", () => {
  assert.equal(needsChoice([OR_GROUP]), true);
  assert.equal(needsChoice([OR_GROUP], { g2: "e3" }), false);
});

test("pendingChoices names the groups still waiting", () => {
  assert.deepEqual(pendingChoices([AND_GROUP, OR_GROUP], {}).map(g => g.id), ["g2"]);
  assert.deepEqual(pendingChoices([AND_GROUP, OR_GROUP], { g2: "e4" }), []);
});

test("an AND group contributes all of its entries", () => {
  assert.deepEqual(resolveEntries([AND_GROUP], {}).map(e => e.id), ["e1", "e2"]);
});

test("an OR group contributes exactly the chosen entry", () => {
  assert.deepEqual(resolveEntries([OR_GROUP], { g2: "e4" }).map(e => e.id), ["e4"]);
});

test("an unchosen OR group contributes nothing rather than guessing", () => {
  assert.deepEqual(resolveEntries([OR_GROUP], {}), []);
});

test("a choice naming an entry that is not in the group contributes nothing", () => {
  assert.deepEqual(resolveEntries([OR_GROUP], { g2: "e99" }), []);
});

test("changes are built at the implant's quality", () => {
  const changes = changesFor([AND_GROUP], {}, 2);
  assert.deepEqual(changes, [
    { key: "system.characteristics.tgh.modifier", mode: 2, value: 5, priority: 20 },
    { key: "system.combat.hitLocations.leftArm.armour", mode: 2, value: 1, priority: 20 }
  ]);
});

test("live entries are separated out and never become changes", () => {
  const group = {
    id: "g3",
    operator: "AND",
    entries: [
      { id: "e5", kind: "testMod", skill: "reflexes", value: 1 },
      { id: "e6", kind: "wounds", value: 2 }
    ]
  };
  assert.deepEqual(changesFor([group], {}, 2).map(c => c.key), ["system.combat.wounds.max"]);
  assert.deepEqual(liveEntries([group], {}).map(e => e.id), ["e5"]);
});

test("empty and malformed mechanics are inert rather than throwing", () => {
  assert.deepEqual(resolveEntries(undefined, undefined), []);
  assert.deepEqual(changesFor([], {}, 2), []);
  assert.deepEqual(resolveEntries([{ id: "g", operator: "AND" }], {}), []);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-entries.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/mechanics/entries.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/mechanics/entries.js`:

```js
/**
 * Which of an implant's entries apply, and what they become.
 *
 * Mechanics are stored as groups:
 *
 *   { id, operator: "AND" | "OR", entries: [ entry, ... ] }
 *
 * An AND group applies all of its entries. An OR group applies exactly one,
 * chosen when the implant is fitted — this is also the mechanism cycle B needs
 * for the book's Best.Q effect choice, which is why it exists now rather than
 * being invented later.
 *
 * An OR group with no choice recorded contributes NOTHING. Defaulting to the
 * first entry would silently give the character a benefit they never picked.
 */

import { LIVE_KINDS, entryToChange } from "./targets.js";

const groups = mechanics => Array.isArray(mechanics) ? mechanics : [];
const entriesOf = group => Array.isArray(group?.entries) ? group.entries : [];
const isOr = group => group?.operator === "OR";

/** An OR group with entries and no recorded choice is still waiting. */
function awaiting(group, chosen) {
  return isOr(group) && entriesOf(group).length > 0 && !chosen?.[group.id];
}

export function pendingChoices(mechanics, chosen = {}) {
  return groups(mechanics).filter(group => awaiting(group, chosen));
}

export function needsChoice(mechanics, chosen = {}) {
  return pendingChoices(mechanics, chosen).length > 0;
}

/** The entries that actually apply, flattened across all groups. */
export function resolveEntries(mechanics, chosen = {}) {
  const out = [];

  for (const group of groups(mechanics)) {
    const entries = entriesOf(group);

    if (!isOr(group)) {
      out.push(...entries);
      continue;
    }

    const pickedId = chosen?.[group.id];
    const picked = entries.find(entry => entry?.id === pickedId);
    // No choice, or a choice naming something that is not in this group: nothing.
    if (picked) out.push(picked);
  }

  return out;
}

/** Active Effect changes for everything that is not a live request. */
export function changesFor(mechanics, chosen = {}, quality = 2) {
  return resolveEntries(mechanics, chosen)
    .map(entry => entryToChange(entry, quality))
    .filter(Boolean);
}

/** The entries that store nothing and are read at roll time. */
export function liveEntries(mechanics, chosen = {}) {
  return resolveEntries(mechanics, chosen).filter(entry => LIVE_KINDS.includes(entry?.kind));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-entries.test.mjs`
Expected: PASS, 10 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-classify.test.mjs tests/implants-rules.test.mjs tests/implants-state.test.mjs tests/implants-targets.test.mjs tests/implants-entries.test.mjs`
Confirm: `pass 52`, `fail 0`. Stop for review.

---

### Task 6: The item type and its registration

The data model, the manifest declaration, and wiring into the module entry point. This is Foundry wiring and is verified in a live world, not by unit test.

**Files:**
- Create: `module/implants/model.js`
- Create: `module/implants/index.js`
- Modify: `module.json` — add `documentTypes.Item.implant`
- Modify: `module/navis-apexialis.js` — import and call `registerImplantModel()` in the `init` hook, `registerImplantSheet()` in `ready`
- Modify: `lang/ru.json`, `lang/en.json` — the `NAVIS.Implant.*` keys

**Interfaces:**
- Consumes: `IMPLANT_TYPE` from `module/implants/state.js`; `SLOTS` from `module/implants/classify.js`.
- Produces:
  - `defineImplantModel() → class|null`
  - `registerImplantModel()`, `registerImplantSheet()`, `reportImplantState()`

- [ ] **Step 1: Declare the type in the manifest**

Modify `module.json`, extending the existing `documentTypes` block that already holds `species` and `subspecies`:

```json
"documentTypes": {
  "Item": {
    "species": {},
    "subspecies": {},
    "implant": {}
  }
}
```

- [ ] **Step 2: Write the data model**

Create `module/implants/model.js`:

```js
/**
 * The Implant item type.
 *
 * Declared in module.json under `documentTypes` — Foundry's own way for a
 * module to add a document sub-type. The system is not patched.
 *
 * The model EXTENDS impmal's `augmetic` model rather than building a schema
 * beside it. That gives cost, encumbrance, quantity, availability, equipped
 * state and notes in the system's own shape, and with them the availability
 * test (`setupAvailabilityTest`) for free. This is the lesson the techno-miracle
 * model records: inherit the system's surface, do not imitate it, or the copy
 * drifts from the original.
 *
 * The class is built inside a function because the model it extends only
 * reaches CONFIG once the system has been evaluated.
 */

import { IMPLANT_TYPE } from "./state.js";
import { SLOTS } from "./classify.js";

const MODULE_ID = "navis-apexialis";

let ImplantModel = null;

function augmeticModel() {
  const augmetic = CONFIG.Item.dataModels?.augmetic;

  if (typeof augmetic !== "function") {
    console.error(
      `${MODULE_ID} | impmal no longer registers an augmetic data model at `
      + "CONFIG.Item.dataModels.augmetic, which the Implant model extends. "
      + "Implants cannot be registered."
    );
    return null;
  }

  return augmetic;
}

export function defineImplantModel() {
  if (ImplantModel) return ImplantModel;

  const AugmeticModel = augmeticModel();
  if (!AugmeticModel) return null;

  const fields = foundry.data.fields;

  const qualityText = () => new fields.SchemaField({
    1: new fields.HTMLField(),
    2: new fields.HTMLField(),
    3: new fields.HTMLField(),
    4: new fields.HTMLField()
  });

  ImplantModel = class ImplantModel extends AugmeticModel {
    static defineSchema() {
      const schema = super.defineSchema();

      schema.rules = new fields.HTMLField();
      schema.page = new fields.StringField({ initial: "" });
      schema.category = new fields.StringField({ initial: "bionic" });

      // The book's Rarity, kept as its own number alongside impmal's
      // four-step `availability`. impmal's scale cannot express the book's
      // "+1 Rarity per extra Best.Q effect" without hitting its ceiling, and
      // cycle B needs the number rather than having to re-derive it.
      schema.rarity = new fields.NumberField({ initial: null, integer: true, nullable: true });

      schema.quality = new fields.NumberField({ initial: 2, integer: true, min: 1, max: 4, nullable: false });
      schema.qualityText = qualityText();

      schema.slot = new fields.StringField({
        initial: "other",
        choices: SLOTS.map(s => s.key)
      });
      schema.location = new fields.StringField({ initial: "internal" });
      schema.side = new fields.StringField({ initial: "", choices: ["", "left", "right"] });

      // The gate. Three booleans, because the book counts fitted and switched
      // on separately (pp. 102, 269) and a damaged implant still occupies its
      // socket.
      schema.installed = new fields.BooleanField({ initial: false });
      schema.disabled = new fields.BooleanField({ initial: false });
      schema.active = new fields.BooleanField({ initial: true });

      // Constructor groups. ObjectField, not a typed schema: the entry kinds
      // are a growing set, and pinning them here would mean a schema migration
      // every time one is added.
      schema.mechanics = new fields.ArrayField(new fields.ObjectField(), { initial: [] });
      schema.chosenEffects = new fields.ObjectField({ initial: {} });

      return schema;
    }
  };

  return ImplantModel;
}

export { IMPLANT_TYPE };
```

- [ ] **Step 3: Write the registration**

Create `module/implants/index.js`:

```js
/**
 * Wiring for the Implant item type.
 *
 * Model at `init`, before any document is prepared; sheet at `ready`, the first
 * moment Foundry's sheet registry exists. The same order as Species and
 * Techno-miracles.
 *
 * Because the type is declared in the manifest, it only reaches a world at
 * launch: enabling the module in a running world registers a model for a type
 * the world does not have. reportImplantState says so out loud rather than
 * leaving the player to wonder why the item will not create.
 */

import { defineImplantModel } from "./model.js";
import { defineImplantSheet } from "./sheet.js";
import { IMPLANT_TYPE } from "./state.js";

const MODULE_ID = "navis-apexialis";

export { IMPLANT_TYPE };

export function registerImplantModel() {
  if (!globalThis.warhammer?.models) {
    console.error(`${MODULE_ID} | warhammer-lib is unavailable; the Implant item type cannot be registered.`);
    return;
  }

  const model = defineImplantModel();
  if (!model) return;

  CONFIG.Item.dataModels[IMPLANT_TYPE] = model;
  CONFIG.Item.typeLabels ??= {};
  CONFIG.Item.typeLabels[IMPLANT_TYPE] = "NAVIS.Implant.Type";
}

export function registerImplantSheet() {
  const sheet = defineImplantSheet();
  if (!sheet) return;

  foundry.documents.collections.Items.registerSheet(MODULE_ID, sheet, {
    types: [IMPLANT_TYPE],
    makeDefault: true,
    label: "NAVIS.Implant.Sheet"
  });
}

export function reportImplantState() {
  const known = game.documentTypes?.Item ?? [];
  if (known.includes(IMPLANT_TYPE)) return;

  console.warn(
    `${MODULE_ID} | the world does not know the item type "${IMPLANT_TYPE}". `
    + "Types declared in a module manifest only reach a world at launch — restart the world."
  );
}
```

- [ ] **Step 4: Wire it into the entry point**

Modify `module/navis-apexialis.js`:

- add `import { registerImplantModel, registerImplantSheet, reportImplantState } from "./implants/index.js";` beside the existing `registerSpeciesModel` import;
- inside `Hooks.once("init", ...)`, call `registerImplantModel();` immediately after `registerSpeciesModel();`;
- inside the existing `Hooks.once("ready", ...)` block (the one that already calls `registerSpeciesSheet`), call `registerImplantSheet();` and then `reportImplantState();`.

- [ ] **Step 5: Add the language keys**

Add to both `lang/ru.json` and `lang/en.json`. Russian values shown; the English file takes the English side of each pair.

```json
"NAVIS.Implant.Type": "Имплант",
"NAVIS.Implant.Sheet": "Лист импланта",
"NAVIS.Implant.Quality": "Уровень",
"NAVIS.Implant.Rarity": "Редкость",
"NAVIS.Implant.Installed": "Установлен",
"NAVIS.Implant.Disabled": "Повреждён",
"NAVIS.Implant.Active": "Включён",
"NAVIS.Implant.Side": "Сторона",
"NAVIS.Implant.Side.left": "Левая",
"NAVIS.Implant.Side.right": "Правая",
"NAVIS.Implant.Slot.cortex": "Лобная кора",
"NAVIS.Implant.Slot.ocular": "Окулярная система",
"NAVIS.Implant.Slot.respiratory": "Дыхательная",
"NAVIS.Implant.Slot.circulatory": "Кровеносная",
"NAVIS.Implant.Slot.skeleton": "Скелет",
"NAVIS.Implant.Slot.skin": "Кожа",
"NAVIS.Implant.Slot.arm": "Руки",
"NAVIS.Implant.Slot.leg": "Ноги",
"NAVIS.Implant.Slot.mechadendrite": "Механодендриты",
"NAVIS.Implant.Slot.fullbody": "Всё тело",
"NAVIS.Implant.Slot.other": "Прочее",
"NAVIS.Implant.OverCap": "Установлено имплантов сверх предела — Помеха на все проверки",
"NAVIS.Surgeon.Title": "Хирургеон",
"NAVIS.Surgeon.Install": "Имплантировать",
"NAVIS.Surgeon.InstallNoTest": "Установить без теста",
"NAVIS.Surgeon.Extract": "Извлечь",
"NAVIS.Surgeon.BothSides": "Обе стороны",
"NAVIS.Surgeon.FromStock": "Со склада",
"NAVIS.Surgery.Adapting": "Адаптация к импланту: осталось суток — {days}"
```

- [ ] **Step 6: Checkpoint**

This task has no unit test — it is registration. Verify in a live world:

1. Restart the world with the module enabled.
2. Create a new Item; confirm **Имплант** appears in the type list.
3. Open the console; confirm no `navis-apexialis |` errors.
4. Confirm an existing `augmetic` item still opens and still counts toward `system.augmetics.max`.

Stop for review.

---

### Task 7: Test modifiers as impmal scripts

A `testMod` entry becomes a **scripted effect** in impmal's own vocabulary, not a Foundry hook of our own. This task is the pure half: turning an entry into the `scriptData` record impmal expects.

**Why scripts and not a hook.** impmal has no effect key for "one more success" or for Advantage — both are decided while a test is prepared, by scripts attached to an effect and fired on a trigger (`actor.runScripts("dialog", args)`), reading and writing `args.fields.SL`, `args.advantage`, `args.disadvantage` and `args.skill`. The module already speaks this vocabulary in `module/config/weapon-trait-effects.js`, which is the working reference to read before starting.

**Advantage and Disadvantage are COUNTERS, not flags.** `args.advantage` is a getter/setter over `fields.advantage` (`impmal.js:90-107`), and `computeState` (`impmal.js:156-168`) compares the two numerically and charges excess as `(advantage − 1) − disadvantage`. Every shipped impmal script uses `args.advantage++`; `args.fields.advantage` appears nowhere in the system. Assigning instead of incrementing silently discards whatever another source — a weapon trait, a second implant, Fate — already contributed.

The payoff is that the gate comes free: a disabled Active Effect runs none of its scripts, so switching an implant off or damaging it silences its test modifiers with no separate bookkeeping.

**Files:**
- Create: `module/implants/test-mods.js`
- Test: `tests/implants-test-mods.test.mjs`

**Interfaces:**
- Consumes: `resolveQualityValue` from `./rules.js`.
- Produces:
  - `TALENT_NAMES` — frozen `{ tuning: string[], sacredCode: string[] }`
  - `talentBonuses(actor) → { talentBonus: number, sacredCode: boolean }`
  - `testModScript(entry, quality) → { label, trigger, script }|null`
  - `CAP_PENALTY_SCRIPT` — the scriptData record for being over the cap

- [ ] **Step 1: Write the failing test**

Create `tests/implants-test-mods.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { TALENT_NAMES, talentBonuses, testModScript, CAP_PENALTY_SCRIPT } from "../module/implants/test-mods.js";

test("the talent names the caps depend on are named, not guessed at", () => {
  assert.ok(TALENT_NAMES.tuning.length > 0);
  assert.ok(TALENT_NAMES.sacredCode.length > 0);
});

test("a matching talent raises the cap bonus", () => {
  const actor = { items: [{ type: "talent", name: TALENT_NAMES.tuning[0] }] };
  assert.deepEqual(talentBonuses(actor), { talentBonus: 1, sacredCode: false });
});

test("talent matching ignores case and is not fooled by a partial name", () => {
  const actor = { items: [{ type: "talent", name: TALENT_NAMES.tuning[0].toUpperCase() }] };
  assert.equal(talentBonuses(actor).talentBonus, 1);
  assert.equal(talentBonuses({ items: [{ type: "talent", name: "Настройка" }] }).talentBonus, 0);
});

test("the Sacred Code is recognised separately", () => {
  const actor = { items: [{ type: "talent", name: TALENT_NAMES.sacredCode[0] }] };
  assert.deepEqual(talentBonuses(actor), { talentBonus: 0, sacredCode: true });
});

test("only talents count — an implant of the same name does not", () => {
  const actor = { items: [{ type: "navis-apexialis.implant", name: TALENT_NAMES.tuning[0] }] };
  assert.deepEqual(talentBonuses(actor), { talentBonus: 0, sacredCode: false });
});

test("an actor with no talents gets no bonus", () => {
  assert.deepEqual(talentBonuses({ items: [] }), { talentBonus: 0, sacredCode: false });
});

test("a successes entry fires on the dialog trigger and adds to SL", () => {
  const script = testModScript({ kind: "testMod", skill: "reflexes", value: 2 }, 2);
  assert.equal(script.trigger, "dialog");
  assert.match(script.script, /args\.fields\.SL \+= 2/);
  assert.match(script.script, /args\.skill !== "reflexes"/);
});

test("an entry with no skill guards on nothing and applies to every test", () => {
  const script = testModScript({ kind: "testMod", value: 1 }, 2);
  assert.doesNotMatch(script.script, /args\.skill !==/);
  assert.match(script.script, /args\.fields\.SL \+= 1/);
});

test("the value is resolved at the implant's quality before it reaches the script", () => {
  const entry = { kind: "testMod", skill: "tech", value: { 1: 1, 2: 2, 3: 3, 4: 4 } };
  assert.match(testModScript(entry, 4).script, /args\.fields\.SL \+= 4/);
});

test("advantage and disadvantage set impmal's own flags", () => {
  assert.match(testModScript({ kind: "testMod", advantage: 1 }, 2).script, /args\.fields\.advantage = true/);
  assert.match(testModScript({ kind: "testMod", advantage: -1 }, 2).script, /args\.fields\.disadvantage = true/);
});

test("an entry of another kind produces no script", () => {
  assert.equal(testModScript({ kind: "wounds", value: 2 }, 2), null);
});

test("an entry that would do nothing produces no script", () => {
  assert.equal(testModScript({ kind: "testMod", value: 0 }, 2), null);
  assert.equal(testModScript({ kind: "testMod" }, 2), null);
});

test("the cap penalty is a dialog script applying Disadvantage", () => {
  assert.equal(CAP_PENALTY_SCRIPT.trigger, "dialog");
  assert.match(CAP_PENALTY_SCRIPT.script, /args\.fields\.disadvantage = true/);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-test-mods.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/test-mods.js'`

- [ ] **Step 3: Write minimal implementation**

Create `module/implants/test-mods.js`:

```js
/**
 * Test modifiers, in impmal's own vocabulary.
 *
 * impmal has no effect key for "one more success" or for Advantage. Both are
 * decided while a test is being prepared, by scripts attached to an effect and
 * fired on a trigger: the actor runs `runScripts("dialog", args)` and the
 * scripts read and write `args.fields.SL`, `args.advantage`,
 * `args.disadvantage` and `args.skill`. The last is a real getter on
 * SkillTestDialog (impmal.js:338), used by impmal's own shipped scripts.
 *
 * So a `testMod` entry becomes a scriptData record on the implant's own effect
 * rather than a hook of ours. Two things follow, both good:
 *
 *   - the gate comes free — a disabled effect runs none of its scripts, so
 *     switching an implant off silences its modifiers with no bookkeeping;
 *   - the player can see the modifier on the effect, where impmal's own
 *     scripted traits appear, instead of it arriving from nowhere.
 *
 * `module/config/weapon-trait-effects.js` is this module's existing example of
 * the same shape — read it alongside this file.
 */

import { resolveQualityValue } from "./rules.js";

/**
 * Talents that move the implant ceilings, by name in both languages.
 *
 * Matched by name because talents are content, not code. A renamed talent stops
 * applying, which is why tools/check-implants.mjs asserts these names exist in
 * the talent pack — the failure is caught at build time rather than at the table.
 */
export const TALENT_NAMES = Object.freeze({
  tuning: Object.freeze(["Настройка Лат", "War Plate Attunement"]),
  sacredCode: Object.freeze(["Таинства Священного Кода", "Mysteries of the Sacred Code"])
});

const nameMatches = (item, names) =>
  names.some(name => name.toLowerCase() === String(item?.name ?? "").toLowerCase());

export function talentBonuses(actor) {
  let talentBonus = 0;
  let sacredCode = false;

  for (const item of actor?.items ?? []) {
    if (item?.type !== "talent") continue;
    if (nameMatches(item, TALENT_NAMES.tuning)) talentBonus += 1;
    if (nameMatches(item, TALENT_NAMES.sacredCode)) sacredCode = true;
  }

  return { talentBonus, sacredCode };
}

/** Being over either ceiling is −30 in the book (p. 269) — Disadvantage under the doctrine. */
export const CAP_PENALTY_SCRIPT = Object.freeze({
  label: "NAVIS.Implant.OverCap",
  trigger: "dialog",
  script: "args.disadvantage++;"
});

/**
 * @param {object} entry   a constructor entry
 * @param {number} quality the implant's level, 1..4
 * @returns {{label: string, trigger: string, script: string}|null}
 */
export function testModScript(entry, quality) {
  if (entry?.kind !== "testMod") return null;

  const successes = resolveQualityValue(entry.value, quality);
  const advantage = Math.sign(Number(entry.advantage) || 0);

  // An entry that would change nothing is not a neutral script — it is a line
  // of code running on every roll for no reason.
  if (!successes && !advantage) return null;

  const lines = [];

  // An entry with no skill speaks to every test, so it needs no guard at all.
  if (entry.skill) lines.push(`if (args.skill !== "${entry.skill}") return;`);

  if (successes) lines.push(`args.fields.SL += ${successes};`);
  if (advantage > 0) lines.push("args.advantage++;");
  if (advantage < 0) lines.push("args.disadvantage++;");

  return {
    label: entry.label || "NAVIS.Implant.TestMod",
    trigger: "dialog",
    script: lines.join("\n")
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test tests/implants-test-mods.test.mjs`
Expected: PASS, 13 tests

- [ ] **Step 5: Checkpoint**

Run: `node --test tests/implants-test-mods.test.mjs`
Confirm: `pass 13`, `fail 0`. Stop for review.

---

### Task 8: Applying and rolling back mechanics

Turning resolved entries into one Active Effect on the implant, carrying both numeric changes and test-modifier scripts, synchronised to the gate — plus the actor-level effect that shows the over-cap penalty.

**Files:**
- Create: `module/implants/mechanics/apply.js`
- Modify: `module/implants/state.js` — accept the talent bonuses
- Modify: `module/implants/index.js` — register the hooks

**Interfaces:**
- Consumes: `changesFor`, `resolveEntries` from `./entries.js`; `isImplantActive`, `implantsOf`, `actorCapState`, `IMPLANT_TYPE` from `../state.js`; `testModScript`, `CAP_PENALTY_SCRIPT`, `talentBonuses` from `../test-mods.js`.
- Produces:
  - `EFFECT_FLAG` — `"mechanicsEffect"`
  - `CAP_FLAG` — `"capPenalty"`
  - `syncImplantMechanics(item) → Promise<void>`
  - `syncCapPenalty(actor) → Promise<void>`
  - `registerImplantMechanicsHooks()`

- [ ] **Step 1: Let `actorCapState` see the talents**

Modify `module/implants/state.js`:

```js
export function actorCapState(actor, { talentBonus = 0, sacredCode = false } = {}) {
  const implants = implantsOf(actor);

  return capState({
    installed: implants.filter(isImplantFitted).length,
    active: implants.filter(isImplantActive).length,
    toughnessBonus: actor?.system?.characteristics?.tgh?.bonus ?? 0,
    talentBonus,
    sacredCode
  });
}
```

The defaults keep `tests/implants-state.test.mjs` passing unchanged — it calls with one argument. Re-run it to confirm before moving on.

- [ ] **Step 2: Write the implementation**

Create `module/implants/mechanics/apply.js`:

```js
/**
 * Constructor entries → one Active Effect, and back again.
 *
 * The effect is created ON THE IMPLANT, never on the actor. That is the whole
 * rollback story: an embedded document leaves with its parent, so removing the
 * implant removes its mechanics however the removal happened — deleted, dragged
 * off, or the actor rebuilt. There is no bookkeeping to get wrong.
 *
 * One effect carries both halves. `changes` are the numeric entries, reaching
 * the same data paths impmal's own effect keys reach. `system.scriptData` are
 * the testMod entries, in the shape impmal runs on its triggers — the same
 * shape module/config/weapon-trait-effects.js already uses.
 *
 * The effect's `disabled` mirrors the gate, which silences BOTH halves at once:
 * a disabled effect contributes no changes and runs no scripts.
 *
 * The cap penalty is the one thing that cannot live on an implant, because it
 * belongs to the actor rather than to any one of them. It gets its own effect
 * on the actor — visible, named, and removed the moment the character is back
 * within the ceiling.
 */

import { changesFor, resolveEntries } from "./entries.js";
import { IMPLANT_TYPE, actorCapState, implantsOf, isImplantActive } from "../state.js";
import { CAP_PENALTY_SCRIPT, talentBonuses, testModScript } from "../test-mods.js";

const MODULE_ID = "navis-apexialis";

/** Marks an effect as this module's, so we only ever replace our own. */
export const EFFECT_FLAG = "mechanicsEffect";
export const CAP_FLAG = "capPenalty";

const ownEffects = (doc, flag) => doc.effects.filter(effect => effect.getFlag(MODULE_ID, flag));

/** impmal reads scripts from an effect's own system data; transferData carries it to the actor. */
const scriptedEffect = (scripts) => ({
  transferData: { documentType: "Item" },
  scriptData: scripts
});

/**
 * Rebuild the implant's effect from its current entries, quality and gate
 * state. Idempotent — safe to call on every relevant update.
 */
export async function syncImplantMechanics(item) {
  if (item?.type !== IMPLANT_TYPE) return;

  const chosen = item.system.chosenEffects ?? {};
  const quality = item.system.quality;

  const changes = changesFor(item.system.mechanics, chosen, quality);
  const scripts = resolveEntries(item.system.mechanics, chosen)
    .map(entry => testModScript(entry, quality))
    .filter(Boolean);

  const existing = ownEffects(item, EFFECT_FLAG);

  // Nothing to express: remove ours and stop. An empty effect on the sheet is
  // something a player has to reason about for no reason.
  if (!changes.length && !scripts.length) {
    if (existing.length) await item.deleteEmbeddedDocuments("ActiveEffect", existing.map(e => e.id));
    return;
  }

  const data = {
    name: item.name,
    img: item.img,
    changes,
    disabled: !isImplantActive(item),
    transfer: true,
    system: scriptedEffect(scripts),
    flags: { [MODULE_ID]: { [EFFECT_FLAG]: true } }
  };

  if (!existing.length) {
    await item.createEmbeddedDocuments("ActiveEffect", [data]);
    return;
  }

  // Replace the first; delete any strays left by an older shape.
  await item.updateEmbeddedDocuments("ActiveEffect", [{ _id: existing[0].id, ...data }]);
  if (existing.length > 1) {
    await item.deleteEmbeddedDocuments("ActiveEffect", existing.slice(1).map(e => e.id));
  }
}

/**
 * The over-cap penalty, as an effect on the actor.
 *
 * It is a real, named, visible effect rather than a silent modifier, because a
 * penalty on every roll that the player cannot see on their sheet is a penalty
 * they will assume is a bug.
 */
export async function syncCapPenalty(actor) {
  if (!actor) return;

  const caps = actorCapState(actor, talentBonuses(actor));
  const existing = ownEffects(actor, CAP_FLAG);
  const wanted = caps.penalty === "disadvantage";

  if (!wanted) {
    if (existing.length) await actor.deleteEmbeddedDocuments("ActiveEffect", existing.map(e => e.id));
    return;
  }

  if (existing.length) return;

  await actor.createEmbeddedDocuments("ActiveEffect", [{
    name: game.i18n.localize("NAVIS.Implant.OverCap"),
    changes: [],
    disabled: false,
    system: { transferData: { documentType: "Actor" }, scriptData: [CAP_PENALTY_SCRIPT] },
    flags: { [MODULE_ID]: { [CAP_FLAG]: true } }
  }]);
}

let registered = false;

export function registerImplantMechanicsHooks() {
  if (registered) return;
  registered = true;

  // Any change to what the entries say, what quality they are read at, or
  // whether the gate is open, rebuilds the effect.
  const WATCHED = ["mechanics", "chosenEffects", "quality", "installed", "disabled", "active"];

  const touchesGate = change =>
    WATCHED.some(key => foundry.utils.hasProperty(change, `system.${key}`));

  Hooks.on("createItem", item => {
    if (item?.type !== IMPLANT_TYPE) return;
    syncImplantMechanics(item).then(() => syncCapPenalty(item.parent));
  });

  Hooks.on("updateItem", (item, change) => {
    if (item?.type !== IMPLANT_TYPE) return;
    if (!touchesGate(change)) return;
    syncImplantMechanics(item).then(() => syncCapPenalty(item.parent));
  });

  Hooks.on("deleteItem", item => {
    if (item?.type !== IMPLANT_TYPE) return;
    syncCapPenalty(item.parent);
  });

  // Toughness damage can put a legal character over the ceiling without any
  // implant changing — that is exactly the case the book calls out on p. 269.
  Hooks.on("updateActor", (actor, change) => {
    if (foundry.utils.hasProperty(change, "system.characteristics.tgh")) syncCapPenalty(actor);
  });
}
```

- [ ] **Step 3: Register the hooks**

Modify `module/implants/index.js`: import `registerImplantMechanicsHooks` from `./mechanics/apply.js` and call it at the end of `registerImplantModel()`. Hooks on documents do not need the sheet registry, so `init` is early enough.

- [ ] **Step 4: Run the tests**

Run: `node --test tests/implants-state.test.mjs tests/implants-test-mods.test.mjs`
Expected: PASS, 21 tests — the state tests unchanged by the new signature.

- [ ] **Step 5: Checkpoint**

Verify in a live world:

1. Create an implant with an AND group holding `characteristic` +5 Toughness and `testMod` +1 success on Reflexes.
2. Confirm one Active Effect appears on the implant, **disabled**, carrying one change and one script.
3. Tick **Установлен**; confirm the effect enables, Toughness rises by 5, and a Reflexes test shows the extra success.
4. Untick **Включён**; confirm both the characteristic and the success bonus disappear.
5. Change **Уровень**; confirm the change's value and the script's number both follow the ladder.
6. Fit more implants than ½ T.b; confirm a named effect **«Установлено имплантов сверх предела»** appears on the actor and every test shows Disadvantage.
7. Switch one off; confirm the effect vanishes.
8. Apply Toughness damage until the bonus drops; confirm the penalty appears without touching any implant — the book's own example.
9. Delete an implant from the actor; confirm its numbers go with it and no effect is left behind.

**Note for the implementer:** the trigger name `dialog` and the field names `args.fields.SL`, `args.advantage`, `args.disadvantage`, `args.skill` were read out of impmal's own `effectScripts` in `impmal.js` and out of `module/config/weapon-trait-effects.js`. If step 3 shows no effect, log `args` inside the script to confirm the shape before changing anything else.

Stop for review.

---
### Task 9: The item sheet

**Files:**
- Create: `module/implants/sheet.js`
- Create: `templates/item/implant.hbs`
- Create: `module/implants/mechanics/constructor.js`
- Create: `templates/item/implant-mechanics.hbs`
- Create: `styles/implants.css`
- Modify: `module.json` — add `styles/implants.css` to the `styles` array

**Interfaces:**
- Consumes: `SLOTS`, `classifyImplant`, `locationForSlot`; `QUALITY_LEVELS`; `ENTRY_KINDS`, `LIVE_KINDS`.
- Produces: `defineImplantSheet() → class|null`.

- [ ] **Step 1: Build the sheet on impmal's own**

Create `module/implants/sheet.js` following `module/species/species-sheet.js` exactly: look the base class up from `CONFIG.Item.sheetClasses.augmetic` — not `talent`, because the augmetic sheet already renders cost, encumbrance, availability and equipped state, which this type inherits — build the class inside a function, and log a clear error if the registry is not ready.

```js
function findBaseSheet() {
  const registered = CONFIG.Item.sheetClasses?.augmetic ?? {};
  const cls = Object.values(registered).find(entry => entry?.cls)?.cls;

  if (!cls) {
    console.error(
      `${MODULE_ID} | no impmal item sheet is registered for "augmetic", so the Implant sheet `
      + "cannot be built on one. This means registerImplantSheet ran before Foundry initialised "
      + "CONFIG.Item.sheetClasses."
    );
  }

  return cls;
}
```

The sheet adds two tabs to whatever the base provides: **Описание** (`templates/item/implant.hbs`) and **Механика** (`templates/item/implant-mechanics.hbs`).

- [ ] **Step 2: The Description tab**

`templates/item/implant.hbs` renders, in this order:

- `system.rules` — HTML editor
- **Уровень** — a select over `QUALITY_LEVELS`, labelled `Уровень 1..4`, with `2` marked as the ordinary article
- the four `system.qualityText.*` HTML editors, each labelled with its level; the sheet prints a hint that only levels differing from 2 need filling
- **Гнездо** (`system.slot`) — a select over `SLOTS`, localised through `NAVIS.Implant.Slot.*`
- **Сторона** (`system.side`) — a select of empty / left / right, shown only when `slot` is `arm`, `leg` or `ocular`
- **Зона** (`system.location`) — a select over the six hit locations plus `internal`, pre-filled by `locationForSlot(slot, side)` when either changes but freely overridable
- **Редкость** (`system.rarity`) — a number
- the three gate checkboxes: **Установлен**, **Повреждён**, **Включён**
- `system.page` — a text field

When `slot` or `side` changes, the sheet writes the classifier's suggestion into `location` **only if the user has not already overridden it** — track that with a `data-touched` attribute set on first manual edit of the location select. Overwriting a deliberate choice is worse than leaving a stale default.

- [ ] **Step 3: The Mechanics tab**

`module/implants/mechanics/constructor.js` renders and edits `system.mechanics`. It is a list of groups; each group has:

- an operator toggle, **И** / **ИЛИ**, with a one-line explanation beside it: «И — применяются все записи. ИЛИ — при установке игрок выбирает одну.»
- a list of entries, each with a kind select over `ENTRY_KINDS`
- per kind, the fields that kind needs:
  - `characteristic` — a select of the nine impmal characteristic keys, plus a value
  - `skill` — a select of the twenty impmal skill keys, plus a value
  - `armour` — a select of the six hit locations, plus a value
  - `armourAll`, `wounds`, `criticals`, `energy` — a value only
  - `speed` — `land` / `fly`, plus a value
  - `encumbrance` — `overburdened` / `restrained`, plus a value
  - `trait`, `talent` — a drop target taking an item, storing `sourceUuid`, `sourceName`, `sourceImg`
  - `testMod` — an optional skill select, a successes value, and an advantage select (Преимущество / — / Помеха)
  - `script` — a code area and an optional throttle
- a **по уровням** toggle on every value field: off stores a plain number, on stores `{1,2,3,4}` and shows four inputs

Every entry gets a stable `id` on creation — `foundry.utils.randomID()` — because `chosenEffects` keys off it and an index would shift when an entry is deleted.

Kinds in `LIVE_KINDS` are marked in the list with a short note: «читается в момент броска, эффекта не создаёт», so an author is not left wondering why no Active Effect appeared.

- [ ] **Step 4: Style it**

`styles/implants.css`, scoped under the sheet's own class so it cannot leak. Follow `styles/biomonitor.css` for the module's visual vocabulary. Add the file to `module.json`'s `styles` array alongside `styles/navis-skin.css`.

- [ ] **Step 5: Checkpoint**

Verify in a live world:

1. Open an implant; confirm both new tabs render and the base tabs still work.
2. Set **Гнездо** to «Руки», **Сторона** to «Левая»; confirm **Зона** becomes `leftArm`.
3. Change **Зона** by hand to `body`, then change **Сторона**; confirm the manual choice is not overwritten.
4. Add an ИЛИ group with two entries; confirm both get distinct ids in the item's source data.
5. Toggle **по уровням** on a value; confirm four inputs appear and the stored shape is `{1,2,3,4}`.

Stop for review.

---

### Task 10: Surgery

**Files:**
- Create: `module/implants/surgery.js`
- Test: `tests/implants-surgery.test.mjs`

**Interfaces:**
- Consumes: nothing from earlier tasks except `IMPLANT_TYPE`.
- Produces:
  - `adaptationDays(roll, toughnessBonus) → number` — pure
  - `fitImplant(actor, item, { test = true }) → Promise<{ fitted: boolean, failed: boolean }>`
  - `extractImplant(actor, item) → Promise<void>`

- [ ] **Step 1: Write the failing test for the pure part**

Create `tests/implants-surgery.test.mjs`:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { adaptationDays } from "../module/implants/surgery.js";

test("adaptation is the roll plus three, less the Toughness bonus (DoomBC p. 49)", () => {
  assert.equal(adaptationDays(10, 0), 13);
  assert.equal(adaptationDays(5, 4), 4);
  assert.equal(adaptationDays(1, 3), 1);
});

test("adaptation never falls below one day, however tough the patient", () => {
  assert.equal(adaptationDays(1, 20), 1);
  assert.equal(adaptationDays(10, 99), 1);
});

test("a missing Toughness bonus does not shorten recovery", () => {
  assert.equal(adaptationDays(4, undefined), 7);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/implants-surgery.test.mjs`
Expected: FAIL — `Cannot find module '../module/implants/surgery.js'`

- [ ] **Step 3: Write the implementation**

Create `module/implants/surgery.js`. The pure function first:

```js
/**
 * Fitting an implant is an operation (DoomBC p. 49): Medicae at −30 — which
 * this module's doctrine renders as Disadvantage — with 1d10 unabsorbed damage
 * on a failure and 1d10+3 − T.b days of adaptation on a success.
 *
 * The test, the damage and the healing are all impmal's own. Nothing here
 * recalculates them; this file decides WHEN they happen and records the result.
 */

import { IMPLANT_TYPE } from "./state.js";

const MODULE_ID = "navis-apexialis";

/** 1d10+3 − T.b days, minimum one. */
export function adaptationDays(roll, toughnessBonus) {
  const bonus = Number.isFinite(toughnessBonus) ? toughnessBonus : 0;
  return Math.max(1, (Number(roll) || 0) + 3 - bonus);
}
```

Then the Foundry half:

- `fitImplant(actor, item, { test = true })`:
  - when `test` is false — set `system.installed = true`, post a short chat note that it was fitted without a test, and return `{ fitted: true, failed: false }`. This is the GM's and starting equipment's path and is the only one that skips the roll.
  - when `test` is true — run `await actor.setupSkillTest("medicae", { appendTitle: ` — ${item.name}`, fields: { disadvantage: true } })`, then read the result.
  - on failure — roll `1d10`, apply it through impmal's own damage application ignoring armour, leave `installed` false, and post the result to chat.
  - on success — set `system.installed = true`, roll `1d10`, compute `adaptationDays(roll, actor.system.characteristics.tgh.bonus)`, store `flags.navis-apexialis.adaptation = { days, since: game.time.worldTime }`, and post a card naming the days.
- `extractImplant(actor, item)` — set `system.installed = false` and leave the item in the character's gear. Extraction is not a test: the book does not make removal a roll, and inventing one would be adding a rule.

**Damage is applied with impmal's own method**, read out of `impmal.js:1502`:

```js
await actor.applyDamage(amount, { ignoreAP: true });
```

`ignoreAP` is what the book's "непоглощаемый урон" means, and armour is the only thing the operation bypasses — the healing and wound engines stay untouched.

`module/technomiracles/activate.js:67` is this codebase's working call to `setupSkillTest`; read it and match its shape rather than inventing one.

- [ ] **Step 4: Resolve pending OR choices before fitting**

An implant whose mechanics contain an unchosen OR group applies nothing from that group — by design, since defaulting to the first entry would hand the character a benefit they never picked. So fitting is the moment to ask.

Before setting `installed`, `fitImplant` calls:

```js
import { pendingChoices } from "./mechanics/entries.js";

async function resolveChoices(item) {
  const chosen = { ...(item.system.chosenEffects ?? {}) };

  for (const group of pendingChoices(item.system.mechanics, chosen)) {
    const buttons = group.entries.map(entry => ({
      action: entry.id,
      label: entry.label || entry.kind
    }));

    const picked = await foundry.applications.api.DialogV2.wait({
      window: { title: item.name },
      content: `<p>${game.i18n.localize("NAVIS.Implant.ChooseEffect")}</p>`,
      buttons
    });

    // A cancelled dialog leaves the group unchosen: the implant is still fitted,
    // that group simply contributes nothing until the choice is made on the sheet.
    if (picked) chosen[group.id] = picked;
  }

  await item.update({ "system.chosenEffects": chosen });
}
```

Add `"NAVIS.Implant.ChooseEffect": "Выберите эффект этого импланта"` to both language files.

The sheet's Mechanics tab also shows which groups are still unchosen, with a button to choose — the dialog can be cancelled, and a choice that can only be made once would strand the implant.

- [ ] **Step 5: Run test to verify it passes**

Run: `node --test tests/implants-surgery.test.mjs`
Expected: PASS, 3 tests

- [ ] **Step 6: Checkpoint**

Verify in a live world:

1. Fit an implant with the test; confirm the Medicae dialog opens at Disadvantage.
2. Force a failure; confirm damage lands, the implant stays unfitted, and a chat card explains why.
3. Force a success; confirm the implant is fitted and the card names the adaptation days.
4. Use **Установить без теста**; confirm no roll and a chat note saying so.
5. Extract; confirm the item stays in gear with `installed` false.
6. Fit an implant carrying an ИЛИ group; confirm the choice dialog appears and only the picked entry applies.
7. Cancel that dialog; confirm the implant is still fitted, the group contributes nothing, and the sheet offers the choice again.

Stop for review.

---

### Task 11: The Surgeon window

**Files:**
- Create: `module/implants/surgeon-app.js`
- Create: `templates/apps/surgeon.hbs`
- Modify: `styles/implants.css`
- Modify: `module/biomonitor/biomonitor-sheet.js` — add the button that opens it

**Interfaces:**
- Consumes: `SLOTS`; `isImplantFitted`, `isImplantActive`, `implantsOf`, `actorCapState`; `fitImplant`, `extractImplant`; the biomonitor's `BODY_SCAN_LAYERS`, `BODY_ORGAN_LAYERS`, `BODY_FIGURE_BOX`.
- Produces: `openSurgeon(actor) → SurgeonWindow|null`.

- [ ] **Step 1: Build the window**

`ApplicationV2` + `HandlebarsApplicationMixin`, one instance per actor keyed on actor id in a module-level `Map`, exactly as `warhammer-dbc/module/apps/surgeon.mjs` does — a second window sharing a DOM id breaks both.

Layout: the biomonitor figure in the centre, built from the existing `BODY_SCAN_LAYERS` and `BODY_ORGAN_LAYERS` assets; slot columns down both sides, one entry per `SLOTS` key, split evenly left and right.

Each slot shows:
- what is fitted there, with its quality, its side, and a switch for `active`
- **Со склада** — implants on the actor that belong in this slot but are not fitted
- the compendium's offerings for this slot, grouped by category

Slots come from the stored `system.slot` field. They are never re-derived from the name at render time: an implant whose slot is empty lands in `other` and is visible there. In the reference system `fullbody` had no slot to land in, so such implants appeared in no slot at all, could never be fitted, and everything they granted was silently rolled back — the numbers on the card were right and there was nowhere to put them.

Paired slots (`arm`, `leg`, `ocular`) hold two, one per side, and offer **Обе стороны** only while both sides are free.

A header line shows `installed / installedCap` and `active / activeCap` from `actorCapState`, turning red and naming `NAVIS.Implant.OverCap` when either is breached.

- [ ] **Step 2: Wire the actions**

- **Имплантировать** from the compendium: create the item on the actor with `slot` and `side` already set, then call `fitImplant(actor, item, { test: true })`.
- **Имплантировать** from stock: call `fitImplant` on the existing item.
- **Установить без теста**: `fitImplant(actor, item, { test: false })`. GM only.
- **Извлечь**: `extractImplant(actor, item)`.
- The `active` switch writes `system.active` directly; Task 7's hook re-syncs the effect.
- Re-render after every action.

- [ ] **Step 3: Add the entry point**

Modify `module/biomonitor/biomonitor-sheet.js` to add a **Хирургеон** button in the biomonitor's header, calling `openSurgeon(actor)`. The biomonitor itself stays read-only — the button opens another window, it does not make the panel editable.

- [ ] **Step 4: Show fitted implants on the biomonitor**

In `module/biomonitor/biomonitor-data.js`, include fitted implants in the view model alongside the native augmetics it already reads, tinted by `system.category`. Display only.

- [ ] **Step 5: Checkpoint**

Verify in a live world:

1. Open the Surgeon from the biomonitor; confirm eleven slots and a figure.
2. Fit an arm implant on the left; confirm it appears in the Руки slot marked left, and on the biomonitor's left arm.
3. Use **Обе стороны**; confirm two implants, one per side, and that the button then disappears.
4. Create an implant with `slot` unset; confirm it appears under **Прочее** and can be fitted.
5. Fit more than ½ T.b; confirm the header turns red and every test shows Disadvantage.
6. Open the Surgeon twice; confirm the second call raises the first window rather than opening a second.

Stop for review.

---

### Task 12: Заряд becomes derived

Reverses a decision recorded in `module/technomiracles/resources.js`, and updates the comment that records it.

**Files:**
- Modify: `module/technomiracles/resources.js`
- Modify: `module/implants/mechanics/apply.js` — recompute on gate changes
- Test: `tests/technomiracles-resources.test.mjs` — extend

**Interfaces:**
- Consumes: `isImplantActive`, `implantsOf`; `resolveQualityValue`.
- Produces: `energyCapacity(actor) → number` exported from `module/technomiracles/resources.js`.

- [ ] **Step 1: Write the failing test**

Add to `tests/technomiracles-resources.test.mjs`:

```js
import { energyCapacity } from "../module/technomiracles/resources.js";

const implantWithEnergy = (value, active = true) => ({
  type: "navis-apexialis.implant",
  system: { installed: true, disabled: false, active, quality: 2, chosenEffects: {},
            mechanics: [{ id: "g", operator: "AND", entries: [{ id: "e", kind: "energy", value }] }] }
});

test("Заряд capacity is the Toughness bonus when nothing adds to it", () => {
  const actor = { system: { characteristics: { tgh: { bonus: 4 } } }, items: [] };
  assert.equal(energyCapacity(actor), 4);
});

test("an active implant with an energy entry raises the capacity", () => {
  const actor = { system: { characteristics: { tgh: { bonus: 4 } } }, items: [implantWithEnergy(5)] };
  assert.equal(energyCapacity(actor), 9);
});

test("a switched-off implant does not", () => {
  const actor = { system: { characteristics: { tgh: { bonus: 4 } } }, items: [implantWithEnergy(5, false)] };
  assert.equal(energyCapacity(actor), 4);
});

test("capacity never goes below zero", () => {
  const actor = { system: { characteristics: { tgh: { bonus: 0 } } }, items: [implantWithEnergy(-5)] };
  assert.equal(energyCapacity(actor), 0);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/technomiracles-resources.test.mjs`
Expected: FAIL — `energyCapacity is not a function`

- [ ] **Step 3: Implement and correct the record**

In `module/technomiracles/resources.js`, add:

```js
import { implantsOf, isImplantActive } from "../implants/state.js";
import { resolveQualityValue } from "../implants/rules.js";

/**
 * Заряд capacity: the Toughness bonus, plus what active implants add.
 *
 * This REVERSES the decision recorded in this file's original header, which
 * said capacity was read once and never recalculated because coupling the
 * techno-miracle pack to the augmetics pack would have bought a single line.
 * That reasoning was sound then and does not hold now: Mechanicum implants
 * bring Manipulus Motive Banks (+5 capacity), Electoo inductors, the War
 * Plate's Actuator Banks, and the techno-miracles that spend the same pool.
 * The `energy` entry kind is the general form of what would have been a
 * special case.
 *
 * Заряд stays one pool, in one place. What changed is that its ceiling moves.
 *
 * It lives in a flag, which an Active Effect cannot reach — so this is
 * recomputed when an implant's gate flips, not by the effects pipeline.
 */
export function energyCapacity(actor) {
  const base = Math.max(0, actor?.system?.characteristics?.tgh?.bonus ?? 0);

  let added = 0;
  for (const item of implantsOf(actor)) {
    if (!isImplantActive(item)) continue;
    for (const group of item.system.mechanics ?? []) {
      for (const entry of group?.entries ?? []) {
        if (entry?.kind === "energy") added += resolveQualityValue(entry.value, item.system.quality);
      }
    }
  }

  return Math.max(0, base + added);
}
```

Then rewrite the file's header comment: delete the sentence *«Ёмкость берётся из бонуса характеристики при первом чтении блока и больше никогда не пересчитывается: установка Двигательных банков сама по себе ничего не поднимет. Так задумано — иначе этот пак и пак аугметики оказались бы связаны кодом ради одной строки.»* and replace it with a pointer to `energyCapacity`. A reversed decision that leaves its old reasoning standing is how the next session gets misled.

Make `readBlock` use `energyCapacity(actor)` for `energy.max` instead of `defaultCapacity(tghBonus)`.

- [ ] **Step 4: Run tests**

Run: `node --test tests/technomiracles-resources.test.mjs`
Expected: PASS, including the four new tests and every pre-existing one.

- [ ] **Step 5: Checkpoint**

Run the whole suite: `node --test tests/`
Confirm no regressions anywhere. Stop for review.

---

### Task 13: The content pipeline and the deletions

**Files:**
- Create: `tools/make-implants.mjs`
- Create: `tools/data/implants-bionics.mjs`
- Create: `tools/check-implants.mjs`
- Create: `tools/import-dbc.mjs`
- Delete: `tools/make-augmetics.mjs`, `tools/check-augmetics.mjs`, `tools/data/augmetics-*.mjs` (six files), `src/packs/items/Augmetics/` (118 files)

- [ ] **Step 1: Back up before deleting anything**

The module is not under version control; these deletions are unrecoverable.

```bash
SCRATCH="$CLAUDE_SCRATCHPAD/augmetics-backup"
mkdir -p "$SCRATCH"
cp -r src/packs/items/Augmetics "$SCRATCH/"
cp tools/make-augmetics.mjs tools/check-augmetics.mjs "$SCRATCH/"
cp tools/data/augmetics-*.mjs "$SCRATCH/"
ls -R "$SCRATCH" | head -20
```

Confirm the backup lists 118 item files, two tools and six data files before proceeding.

- [ ] **Step 2: Write the source format for one family**

Create `tools/data/implants-bionics.mjs` — the bionics of DoomBC pp. 263–264, authored fresh from the book. Entry shape, which `make-implants.mjs` consumes:

```js
export const BIONICS = [
  {
    name: "Бионические ноги: гусеницы",
    rarity: 0,                    // the book's R number
    availability: "common",       // impmal's own enum
    cost: 600,
    page: "DoomBC стр. 263",
    slot: "leg",                  // omit to let the classifier decide
    text: "<p>…книжное описание…</p>",
    rules: "<p>…что имплант делает, словарём impmal…</p>",
    levels: { 1: "…", 3: "…", 4: "…" },   // only levels that differ from 2
    mechanics: [
      {
        operator: "AND",
        entries: [
          { kind: "encumbrance", key: "overburdened", value: 2 },
          { kind: "testMod", skill: "reflexes", advantage: -1 }
        ]
      }
    ]
  }
];
```

Every entry gets its `id` assigned by the generator, not written by hand — `mechanics` entries without ids would break `chosenEffects`.

- [ ] **Step 3: Write the generator**

Create `tools/make-implants.mjs`, modelled on the deleted `make-augmetics.mjs` — read its backup in the scratchpad for the folder scheme, the id scheme and the `describe()` assembly, all of which carry over. Changes from the original:

- emits `"type": "navis-apexialis.implant"`, not `"augmetic"`
- id prefix `navisImp` + 8 digits = 16 characters exactly
- folder id prefix `navisImpFld` + 5 digits = 16 characters exactly
- writes `system.rules`, `system.qualityText.*` (levels split into their own fields rather than concatenated into notes), `system.slot`, `system.location`, `system.rarity`, `system.page` and `system.mechanics`
- fills `slot` from the entry, or from `classifyImplant(name, category, "")` when the entry omits it
- fills `location` from `locationForSlot(slot, side)`
- assigns `foundry.utils.randomID()`-shaped 16-character ids to every mechanics group and entry, deterministically from the item id and index so regeneration is stable
- output directory `src/packs/items/Implants/`

- [ ] **Step 4: Write the audit**

Create `tools/check-implants.mjs`, modelled on the backed-up `check-augmetics.mjs`. It asserts, over every generated file:

- `_id` is exactly 16 alphanumeric characters, and so is every folder id and every mechanics group and entry id
- `type` is `navis-apexialis.implant`
- `slot` is one of the eleven, and `location` is what `locationForSlot(slot, side)` gives — they must agree
- `rarity` is a number and `availability` is one of impmal's four
- at least one `qualityText` level is filled
- either `mechanics` is non-empty or the entry carries `proseOnly: true`, and it reports every item that is prose only, by name, as a list rather than passing over them silently
- the talent names in `TALENT_NAMES` exist in `src/packs/talents`

- [ ] **Step 5: Write the import aid**

Create `tools/import-dbc.mjs`. It reads `D:\Foundry\DoomCrusade\Data\dumbc\warhammer-dbc\packs-src\implants\**\*.json` and prints, per implant, the machine-readable hints it carries: `system.effects` numbers and each Active Effect change's key and value, mapped through a small table onto this module's entry kinds where the mapping is obvious.

It writes nothing. It is an aid to authoring, not a content source: the prose is written fresh from the book. Say so in the file header, so no later session mistakes it for the pipeline.

- [ ] **Step 6: Delete the old pipeline**

```bash
rm -rf src/packs/items/Augmetics
rm tools/make-augmetics.mjs tools/check-augmetics.mjs
rm tools/data/augmetics-bionics.mjs tools/data/augmetics-cybernetics.mjs \
   tools/data/augmetics-mechadendrites.mjs tools/data/augmetics-mechanicum.mjs \
   tools/data/augmetics-psybernetics.mjs tools/data/augmetics-skitarii.mjs
```

Then grep for anything still referencing them and fix each hit:

```bash
grep -rn "augmetics-\|make-augmetics\|check-augmetics" --include="*.mjs" --include="*.js" --include="*.json" . | grep -v node_modules
```

- [ ] **Step 7: Generate and verify**

```bash
node tools/make-implants.mjs
node tools/check-implants.mjs
node tools/build.mjs
node --test tests/
```

Expected: the generator reports the bionics count, the audit passes with a list of prose-only items, the build packs `navis-items`, and every test passes.

- [ ] **Step 8: Checkpoint**

Verify in a live world: open the Items compendium, confirm the Implants folder tree, open one bionic leg, confirm its quality ladder renders and its mechanics entries are present, and fit it through the Surgeon.

Stop for review. **The remaining five families — cybernetics, mechadendrites, Mechanicum, Skitarii, psybernetics — are Task 14 and are authored the same way, one file per family, once this one family has proven the format end to end.**

---

### Task 14: The remaining five families

**Files:**
- Create: `tools/data/implants-cybernetics.mjs` (DoomBC pp. 264–268)
- Create: `tools/data/implants-mechadendrites.mjs`
- Create: `tools/data/implants-mechanicum.mjs` (pp. 271–275)
- Create: `tools/data/implants-skitarii.mjs` (pp. 269–270)
- Create: `tools/data/implants-psybernetics.mjs`
- Modify: `tools/make-implants.mjs` — add the five to `SECTIONS` and the folder table

Authored exactly as Task 13 Step 2, one file per family, each entry carrying `text`, `rules`, `levels` and `mechanics`. The conversion doctrine in Global Constraints applies without exception.

Mechanicum entries that touch Заряд use `kind: "energy"` and are the reason Task 12 exists — Manipulus Motive Banks is `{ kind: "energy", value: 5 }`.

The Skitarii War Plate is `slot: "fullbody"` and is the test of Task 11 Step 4.

Anything the doctrine says is prose only — Haywire, Ноосфера — carries `proseOnly: true` so the audit reports it rather than failing on it.

- [ ] **Step 1: Author one family, regenerate, audit**

For each family in turn: write the data file, add it to `SECTIONS`, then run

```bash
node tools/make-implants.mjs && node tools/check-implants.mjs
```

Expected: the count rises by that family's size and the audit passes.

- [ ] **Step 2: Build and test**

```bash
node tools/build.mjs
node --test tests/
```

- [ ] **Step 3: Checkpoint**

Verify in a live world: the full folder tree, one implant opened from each of the six families, and a Mechanicum implant with an `energy` entry raising the Заряд ceiling on the character sheet when fitted and lowering it when switched off.

Stop for review.

---

## Coverage against the spec

| Spec section | Task |
|---|---|
| The item type | 6 |
| Why `location` and `slot` are both needed | 1, 9 |
| Why `active` is separate from `installed` | 3 |
| The gate | 3, 8 |
| Limits | 2, 7, 8 |
| Quality | 2, 9 |
| The Mechanics constructor | 4, 5, 8, 9 |
| OR groups and the choice on fitting | 5, 9, 10 |
| `testMod` as a live request | 7, 8 |
| `energy` reverses an earlier decision | 12 |
| Surgery | 10 |
| The Surgeon window | 11 |
| The body-zone classifier | 1 |
| Content pipeline | 13, 14 |
| Deletions | 13 |
| Testing | throughout; pure files unit-tested, Foundry wiring checked live |
| Out of scope — Drukhari, gene-seed, Best.Q rule, Haywire, Noosphere, native `augmetic` | not implemented, asserted by Task 13's audit |

**Known limitation, carried deliberately:** the `script` entry kind can be *authored* on the sheet (Task 9) and is recognised as a live kind (Task 4), but nothing in this plan *executes* one. No implant in cycle A's six families needs free JS — every effect they carry is a number or a test modifier. Executing scripts, with the throttle and pool-cost gating the reference system wraps around them, arrives with the families that need it. `tools/check-implants.mjs` fails the build if a generated implant carries a `script` entry, so this cannot go unnoticed.
